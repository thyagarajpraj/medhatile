export type {
  DifficultyConfig,
  DifficultyMode,
  GameState,
  LevelConfig,
  Phase,
} from "@medhatile/types";

export type TileState = "default" | "reveal" | "selected_correct" | "answer" | "wrong";
