# Mobile App Scaffold

This folder contains the React Native CLI scaffold requested for the MedhaTile monorepo.

Native `android/` and `ios/` projects are not generated in this environment because the React Native CLI init step and dependency installation have not been run yet. After installing `pnpm` and workspace dependencies, run a React Native CLI bootstrap from `apps/mobile` to generate the native folders if needed.
